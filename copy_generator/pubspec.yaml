name: copy_generator
version: 0.1.3+1
authors:
  - tbm98 <tbm98.dev@gmail.com>
description: Automatically generating `copyWith` extensions code for classes with `@Copyable()` annotation.
repository: https://github.com/tbm98/copy
homepage: https://github.com/tbm98/copy
issue_tracker: https://github.com/tbm98/copy/issues

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  analyzer: ^0.40.4
  build: ^1.5.0
  source_gen: ^0.9.7+1
  copy:
    git:
      url: git://github.com/snowYarik/copy
      path: copy

dev_dependencies:
  build_runner: ^1.10.3
  build_test: ^1.2.2
